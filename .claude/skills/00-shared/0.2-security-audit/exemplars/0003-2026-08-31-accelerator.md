# Security audit — accelerator — 2026-08-31

**Score: 100/100 — Excellent** (`chaitu67/accelerator`, private)

First `accelerator`-only run — `0001`/`0002` audited `databricks` + `accelerator` jointly, with
every deduction in both landing on `databricks`. Re-run scoped to `accelerator` alone in response
to a direct question about this repo specifically, and to account for what's landed here since
`0002`: the `5.4-create-volume` skill, the `06-organization-setup`/`07-business-unit-setup`/
`08-lob-setup`/`09-environment-setup` skill groups (16 new files), and a rewritten top-level
`README.md`.

## Actionable, not yet fixed

None.

## Checked and clean

- **Hardcoded secrets**: `git grep` across all tracked files (incl. the new 06–09 skill files and
  the rewritten `README.md`) for AWS access-key patterns, private-key headers, and
  secret/token/password/apikey-shaped assignments — none found.
- **Personal/sensitive data**: grepped for email addresses and 12-digit account-ID-shaped strings
  — the only hits are the *prose* in this skill's own past exemplars (`0001`/`0002`) describing a
  finding that belongs to `databricks`, not a leak in `accelerator` itself.
- **GitHub Actions workflows**: none exist in this repo (no `.github/workflows/`) — nothing to
  audit for permissions scoping, action pinning, or trigger type. `accelerator` doesn't run its
  own CI; it only scaffolds CI/CD *for* the sibling `databricks` repo via `04-github-cicd`.
- **Terraform / IaC**: none tracked in this repo (`accelerator` describes and drives Terraform in
  the sibling repo; it doesn't hold any of its own) — nothing for Checkov or S3-hardening checks
  to apply to.
- **Local venv**: `.venv/` is gitignored and confirmed not tracked (`git ls-files` — zero matches)
  — `02-setup-local-env`'s Python venv/dependency install never lands in the repo.
- **Repo visibility**: confirmed **private** (`gh repo view` — `isPrivate: true`), unlike
  `databricks` which is public — the smaller, non-infrastructure repo is also the more locked-down
  one.

## Not applicable (GitHub plan limitation, not a finding)

- `security_and_analysis` (secret scanning / Dependabot / push protection) and branch protection
  are both unavailable via the API for a **private** repo on a free GitHub plan
  (`403 Upgrade to GitHub Pro or make this repository public`) — this isn't a misconfiguration the
  owner left off, it's a feature gate. Making the repo public to unlock these would trade a real,
  free win (secret scanning) for a real loss (any hardcoded secret becomes publicly visible on
  push, and the "checked and clean" secrets sweep above would need to run on every commit, not
  just on-demand) — not recommending that trade for a repo with no CI/CD and no infrastructure
  credentials of its own to protect.

## Accepted risk ledger

None carried forward — every ledger entry in `0001`/`0002` (CI service-principal `account_admin`,
`databricks`'s public-repo OIDC trust scope, `production` environment bypass settings, `main`
branch protection's `enforce_admins: false`) is a `databricks`-repo condition with no equivalent
surface in `accelerator` (no CI, no cloud credentials, no `production` environment, no Terraform
state to protect here). Nothing to re-confirm or track against this repo specifically.
